export enum RouteNames {
  Home = "/",
  MovieDetails = "/movies/:movieId",
  Login = "/login",
  Register = "/register",
  Favorites = "/favorites",
  Settings = "/settings",
  Actor = "/actor/:actorId",
}
