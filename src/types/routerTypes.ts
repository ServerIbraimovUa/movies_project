export enum RouteNames {
  Home = "/",
  MovieDetails = "/movie/:movieId",
  Login = "/login",
  Register = "/register",
  Favorites = "/favorites",
  Settings = "/settings",
  Actor = "/actor/:actorId",
}
